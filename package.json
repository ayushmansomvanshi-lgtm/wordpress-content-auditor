{
  "name": "radish-wordpress-auditor",
  "version": "4.4.1",
  "private": true,
  "description": "Radish audits WordPress content, featured images, main-page image links, authors, writing, headings, performance and UI patterns, including password-protected staging sites.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "playwright install chromium",
    "setup:browsers": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "1.0.0",
    "chrome-launcher": "1.2.0",
    "express": "5.1.0",
    "lighthouse": "12.3.0",
    "playwright": "1.55.0"
  }
}
